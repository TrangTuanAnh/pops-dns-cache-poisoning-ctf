# Override compose cho stage 3: them attacker container chay
# stage3_flag_b.py (probe POPS lab API).
name: pops-ctf-stage3

services:
  attacker:
    build:
      context: .
      dockerfile: attacker/Dockerfile
    networks:
      ctf_net:
        ipv4_address: 10.0.3.50
    ports:
      - "8383:8080"
