# spoof-helper

Lab convenience endpoint for spoofed UDP experiments. It defaults to dry-run
mode and only records request payloads.

Do not enable real raw packet sending until the stage threat model and network
limits are locked down.

