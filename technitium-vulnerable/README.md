# Technitium vulnerable target

Stage 3 is planned around Technitium DNS Server v7.0 for CVE-2021-43105.
This directory currently contains a placeholder container only.

Before the real lab:

- locate a reproducible v7.0 artifact or build from the tagged source release;
- store the retrieval/build notes in this README;
- expose DNS on port 53 and the management UI only if needed for setup;
- keep player-facing access aligned with the off-path threat model.

