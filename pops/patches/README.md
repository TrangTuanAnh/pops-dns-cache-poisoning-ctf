# POPS lab patches

Keep small, reviewable patches here instead of editing imported POPS source
directly. Proposed patches from the proposal:

- `lab-logging.patch`: expose enough logs for debugging and player-facing evidence.
- `stage3-naive-check.patch`: optional naive bailiwick checker variant for Stage 3B.

