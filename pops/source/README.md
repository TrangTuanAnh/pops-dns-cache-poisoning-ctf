# POPS source placeholder

Put the POPS implementation artifact from Zenodo here.

Expected next steps:

1. Download and verify the artifact hash.
2. Record the exact artifact version in the project report.
3. Replace `../entrypoint.sh` with the real transparent proxy startup command.
4. Keep any local changes as patch files under `../patches/`.

